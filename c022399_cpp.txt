#include <stdio.h>
#include <math.h>

main()
{
    int i;
    double x, y, r;
    for(i = -180; i < 180; i++)
    {
        r = (i / 180) * 3.14;
        x = r;
        y = sin(x);
        printf("x=%f,", x);
        printf("y=%f", y);
        printf("\n");
    }
}
